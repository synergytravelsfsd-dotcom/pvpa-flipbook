# Database — local development (SQLite)
DATABASE_URL="file:./data/pvpa.db"

# Production: use Vercel Postgres connection string instead of SQLite
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/pvpa?sslmode=require"

# App: PVPA Digital Flipbook
NEXT_PUBLIC_BASE_URL="https://pvpa-digital-flipbook.vercel.app"

# Vercel Blob — auto-injected when Blob storage is linked to the project
# BLOB_READ_WRITE_TOKEN=""
